# HAI Tech Lab - 全量 TODO 清单

**状态**: 持续更新中
**最近更新**: 2026-01-20

本清单基于 PRD、阶段模板与当前系统现状整理，分为 P0 (必须/MVP)、P1 (重要/增强) 和 P2 (规划/未来) 三个优先级。

## P0: MVP 完善 (立即执行)
*目标：确保“立项-过程-结题”全流程闭环顺畅，数据字段完全对齐，基础体验达标。*

### 1. 核心系统 (Core)
- [x] **API 前缀统一**
  - [x] 前后端统一为 `/api/v1` 前缀。
- [x] **基础认证与权限**
  - [x] 实现注册/登录/当前用户接口（JWT）。
  - [x] 角色权限隔离（学生仅可访问自己的项目，教师可管理全部）。
- [x] **数据库结构补齐**
  - [x] 新增 `users / teams / team_members / attachments / feedback` 表。
  - [x] 附件写入 `attachments` 表，并与 `submissions` 关联。
- [x] **历史数据迁移脚本**
  - [x] 提供 `submissions.jsonl` → SQLite 迁移脚本。
- [x] **缺失文件补齐**
  - [x] 输出缺失文件清单（`storage/logs/missing-uploads-2026-01-20.csv`）。
  - [x] 补齐 `project1/2/5` 的缺失附件并重新执行迁移脚本。
- [x] **状态流转规则校验**
  - [x] 仅允许合法状态迁移（避免任意跳转）。
- [x] **阶段字段强校验**
  - [x] 按阶段模板字段进行必填/格式校验。
- [x] **上传安全校验强化**
  - [x] 类型白名单 + 扩展名匹配 + 超限拦截。
- [x] **日志与审计**
  - [x] 关键操作与错误日志可追溯（便于回滚/审计）。

### 2. 学生端 (Student Dashboard)
- [x] **状态可视化优化**
  - [x] 在“我的项目”列表和详情页，将纯文本状态（`submitted`, `reviewing` 等）改为 **彩色 Badge 标签**（如：审核中-黄色，通过-绿色，驳回/需修改-红色）。
- [x] **“去修改”引导闭环**
  - [x] 当某次提交（如里程碑1）被老师标记为 `needs_changes`（需修改）时，在详情页该记录旁显示 **“重新提交”按钮**。
  - [x] 点击后自动跳转到“提交成果”Tab，并预填充 `Project ID` 和 `提交类型`。
- [x] **空状态引导**
  - [x] 当“我的项目”列表为空时，显示醒目的 **“新建项目”引导卡片/按钮**，而不是仅显示文本“暂无数据”。

### 3. 老师端 (Teacher Dashboard)
- [x] **教师端数据管理**
  - [x] 项目筛选列表与状态统计面板。
  - [x] 项目状态更新 + 状态日志查看。
  - [x] 阶段提交反馈保存。
  - [x] CSV 导出（支持筛选）。
- [x] **批量附件下载**
  - [x] 按项目/按筛选打包下载全部附件。
- [x] **项目归档导出**
  - [x] 一键导出项目资料包（提交记录 + 附件）。

### 4. 交互体验 (UX)
- [x] **文件上传校验与反馈**
  - [x] 在 `createProjectForm` 和 `submissionForm` 的文件输入框旁，增加 **格式与大小提示**（"支持 PDF, Word, ZIP, 图片。单文件限 200MB"）。
  - [x] 在选择文件后，显示 **“已选 X 个文件”** 的文本反馈（目前原生 input 比较隐晦）。
- [x] **时间戳格式化**
  - [x] 将数据库返回的 ISO 时间字符串（`2026-01-20T...`）格式化为易读格式（如 `2026/01/20 14:30`）。

### 5. 展示端 (Showcase)
- [x] **展示墙空状态与加载优化**
  - [x] 确保没有展示项目时显示友好的插画或提示。
  - [x] 确保展示卡片中的“团队成员”字段如果在数据库中是空的，能显示默认值（如“未填写”）。

---

## P1: 核心功能增强 (近期计划)
*目标：提升管理效率，增强数据的结构化程度。*

### 1. 账号与权限
- [x] **用户管理 (Admin Lite)**
  - [x] 在老师端增加一个隐藏或独立的入口，用于 **重置学生密码** 或 **查看注册用户列表**。
- [x] **个人资料修改**
  - [x] 允许学生/老师修改自己的昵称或头像（目前仅支持注册时设置）。

### 2. 数据结构化
- [x] **团队成员关联**
  - [x] 目前 `team_members` 是纯文本。
  - [x] **后端**：设计 `project_members` 表。
  - [x] **前端**：在立项时，支持通过搜索邮箱/姓名添加真实的系统用户作为组员。

### 3. Gitea 集成 (基础)
- [x] **Gitea 链接字段**
  - [x] 在 `projects` 表中增加 `gitea_repo_url` 字段。
  - [x] 在立项或项目详情页，允许学生填入该项目的 Gitea 仓库地址。
  - [x] 在老师评审端提供点击跳转 Gitea 的链接。

---

## P2: 生态与高级特性 (未来规划)
*目标：打造社区氛围，实现自动化流转。*

### 1. 评审体系升级
- [x] **多角色评审**
  - [x] 引入“评委”角色，支持对同一个项目进行打分（非简单的文本反馈）。
- [x] **评分标准配置**
  - [x] 后台支持配置不同阶段（如开题、结题）的评分维度（创新性、完成度、展示效果）。

### 2. 社区功能
- [x] **项目讨论区**
  - [x] 在项目详情页下方增加评论区，允许同学间互相留言。
- [x] **全站公告系统**
  - [x] 首页顶部增加公告栏（如“中期答辩将于下周五举行”）。

### 3. 自动化
- [x] **自动创建 Gitea 仓库**
  - [x] 立项通过后，系统自动调用 Gitea API 为团队创建仓库并分配权限。

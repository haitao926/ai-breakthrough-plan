# HAI Tech Lab - Web (Vue)

这是 Vue 3 + Vite 重构的新前端工程，作为现有 `apps/web` 的逐步替代。

## 目录
- `src/pages/` 页面入口（workspace/tools/projects/teacher 等）
- `src/modules/` 业务模块（立项、实施、审核等）
- `src/api/` API client 与配置加载

## 开发
1. 安装依赖
2. 运行 `npm run dev`

## 构建
`npm run build` 输出到 `dist/`，可用于生产部署。
